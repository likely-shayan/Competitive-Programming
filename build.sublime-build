{
    "cmd": "g++ -std=c++2b -Wall -O3 -DLOCAL_JUDGE program.cpp -o program.out && timeout 2s ./program.out",
    "file_regex": "^(...*?):([0-9]*):?([0-9]*)",
    "working_dir": "${file_path}",
    "selector": "source.c++",
    "shell": true
}
